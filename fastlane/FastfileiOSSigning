platform :ios do
  lane :generate_certificates do |options|
    produce(
      # To completec
    )
    match(
      # To completec
    )
    match(
      # To completec
    )
  end


  lane :download_certificates do |options|
    match(
      # To completec
    )
    match(
      # To completec
    )
  end
end
